package blade.addon.utils.interfaces;

public interface ArmourStandHolder {

    boolean scanned = false;

    boolean blade_addons$hasBeenScanned();

    void blade_addons$setScanned(boolean value);
}
